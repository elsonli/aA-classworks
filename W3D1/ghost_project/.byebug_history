possibleWords.sample
losingWords.sample
losingWords = possibleWords.select { |word| word.length == 3 }
losingWords = possibleWords.select { |word| word.length == 4 }
@fragment
fragment
losingWords = possibleWords.select { |word| word.length == (fragment.length + 1) }
