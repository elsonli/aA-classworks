import React from "react";
import ReactDOM from "react-dom";

// Document Ready Callback
document.addEventListener("DOMContentLoaded", () => {
  const root = document.getElementById("root");
  ReactDOM.render(<h1>React Not Ded</h1>, root);
});